---
import { ClientRouter } from 'astro:transitions'
import MainNav from '../components/MainNav.astro'
import '../styles/global.css'

interface Props {
  title: string
  description?: string
}

const {
  title,
  description = 'Chris Lacey — full-stack AI product engineer. Close the gap between intent and outcome.',
} = Astro.props
const currentPath = Astro.url.pathname
const siteUrl = Astro.site || new URL(Astro.url.origin)
const ogImageUrl = new URL('/api/og.png', siteUrl)
const canonicalUrl = new URL(Astro.url.pathname, siteUrl)

// JSON-LD structured data
const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'WebSite',
      '@id': `${siteUrl}#website`,
      url: siteUrl.toString(),
      name: 'Chris Lacey',
      description,
    },
    {
      '@type': 'Person',
      '@id': `${siteUrl}#person`,
      name: 'Chris Lacey',
      jobTitle: 'Full-stack AI product engineer',
      url: siteUrl.toString(),
      image: ogImageUrl.toString(),
      sameAs: ['https://github.com/chrislacey89', 'https://linkedin.com/in/chrislacey'],
    },
  ],
}
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Preload Switzer variable display face -->
    <link
      rel="preload"
      href="/fonts/Switzer-Variable.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />

    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageUrl.toString()} />
    <meta property="og:url" content={Astro.url.toString()} />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl.toString()} />

    <!-- Canonical URL -->
    <link rel="canonical" href={canonicalUrl.toString()} />

    <!-- JSON-LD Structured Data -->
    <script is:inline type="application/ld+json" set:html={JSON.stringify(jsonLd)} />

    <!-- Read persisted theme before paint to avoid flash on first load -->
    <script is:inline>
      ;(function () {
        try {
          var stored = localStorage.getItem('cl-theme')
          if (stored === 'dark' || stored === 'light') {
            document.documentElement.setAttribute('data-theme', stored)
          } else {
            document.documentElement.setAttribute('data-theme', 'light')
          }
        } catch (_) {
          document.documentElement.setAttribute('data-theme', 'light')
        }
      })()
    </script>

    <ClientRouter />
  </head>
  <body class="min-h-screen">
    <a href="#main-content" class="skip-link">Skip to main content</a>
    <MainNav currentPath={currentPath} />
    <main id="main-content">
      <slot />
    </main>
    <script>
      // Native-style scroll restoration for the "← All projects" link on
      // project detail pages. When the user clicks a project row on
      // /projects, we remember which detail URL they're about to open. If
      // the back link on THAT detail page is clicked, we call history.back()
      // — which triggers the browser's own popstate scroll restoration, the
      // same thing the browser Back button uses. For any other entry point
      // (direct link, detail→detail via Next, etc.) we leave the link alone
      // so it navigates forward to /projects normally.
      const CL_BACK_FLAG = 'cl-projects-to-detail'

      document.addEventListener('click', (event) => {
        const target = event.target as HTMLElement | null
        if (!target) return

        const leadRow = target.closest<HTMLAnchorElement>('a.lead-row')
        if (leadRow && window.location.pathname === '/projects') {
          try {
            const to = new URL(leadRow.href).pathname.replace(/\/$/, '')
            sessionStorage.setItem(CL_BACK_FLAG, to)
          } catch {}
          return
        }

        const backLink = target.closest<HTMLAnchorElement>('a.back')
        if (!backLink) return
        if (backLink.getAttribute('href') !== '/projects') return

        const flag = sessionStorage.getItem(CL_BACK_FLAG)
        const here = window.location.pathname.replace(/\/$/, '')
        if (flag && flag === here) {
          event.preventDefault()
          sessionStorage.removeItem(CL_BACK_FLAG)
          window.history.back()
        }
      })
    </script>
  </body>
</html>

<style is:global>
  .skip-link {
    position: absolute;
    top: -100%;
    left: 50%;
    transform: translateX(-50%);
    background: var(--color-foreground);
    color: var(--color-background);
    padding: 0.75rem 1.5rem;
    border-radius: 0 0 0.5rem 0.5rem;
    font-weight: 600;
    z-index: 9999;
    transition: top 0.2s ease;
  }

  .skip-link:focus {
    top: 0;
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
  }
</style>
